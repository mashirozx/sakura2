mutation CREATE_COMMENT($input: CreateCommentInput!) {
  createComment(input: $input) {
    comment {
      id
      content
      author {
        ... on CommentAuthor {
          name
        }
      }
    }
  }
}
